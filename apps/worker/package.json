{
  "name": "@ballast/worker",
  "version": "0.4.0",
  "private": true,
  "description": "Ballast background worker: check ingestion, migration rehearsals, retention.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@ballast/core": "0.4.0",
    "@ballast/db": "0.4.0",
    "pg": "^8.13.0",
    "pg-boss": "^10.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3"
  }
}
